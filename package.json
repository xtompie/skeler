{
  "scripts": {
    "install": "(cd resources/admin && npm install) && (cd resources/front && npm install)",
    "i": "npm run install",
    "dev": "(cd resources/admin && npm run dev) && (cd resources/front && npm run dev)",
    "prod": "(cd resources/admin && npm run prod) && (cd resources/front && npm run prod)"
  }
}
